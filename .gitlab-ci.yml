image: node:11

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules

before_script:
  - node -v
  - npm install -g jest eslint

stages:
  - test

jest:
  stage: test
  script:
    - yarn test

eslint:
  stage: test
  script:
    - yarn lint